{
  "name": "root",
  "version": "1.0.0-alpha",
  "private": true,
  "scripts": {
    "start": "yarn workspace example-app dev",
    "build": "lerna run build --stream",
    "test": "cross-env CI=true lerna run test  --stream --parallel",
    "lint": "lerna run lint --stream"
  },
  "workspaces": [
    "packages/*",
    "example"
  ],
  "devDependencies": {
    "cross-env": "^7.0.3",
    "lerna": "^6.0.3",
    "prettier": "^2.7.1",
    "typescript": "^4.9.3"
  }
}
